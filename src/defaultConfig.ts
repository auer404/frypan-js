const defaultConfig = {

    initialZoomFactor: 1,
    minZoomFactor: 1,
    maxZoomFactor: 2,
    intensityCurve: 0.025,

    autoResize: true,
    preserveCenterOnResize: true,

    enableZoom: true,
    enableMouseWheel: false,
    mouseWheelTakeover: false,
    zoomKey: false,
    autoZoomCursor: false,

    enablePan: true,
    enableMouseDrag: false,
    dragKey: false,
    autoDragCursor: false
}

export default defaultConfig;